/* eslint-disable no-var */
/* --- global variable --- */

declare global {
/*  ↓↓↓ "var" is important  */  
    var configDevice: ConfigDeviceDto;
    var globalSocket;
    var ports;
    var plc;
    var itemGroup;
    var intervals;
    var authEmmiter;
}